import React from 'react';

const FormValidationErrorMessage = ({ error }) => {
    return <span className="form-validation-error-message">{error}</span>;
};

export default FormValidationErrorMessage;
